from metrics import (
    compute_claim_accuracy,
    compute_hallucination_rate,
    detect_hallucination
)

def evaluate_model(outputs, contexts, labels):
    """
    Placeholder for running evaluation over a dataset.
    """
    raise NotImplementedError
